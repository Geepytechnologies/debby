import React from "react";

type Props = {};

const Hero = (props: Props) => {
  return (
    <div className=" w-full h-[500px]">
      <img src="/hero.png" />
    </div>
  );
};

export default Hero;
